<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!--    namespace接口全路径-->
<mapper namespace="com.atguigu.gmall.product.mapper.BaseAttrInfoMapper">
<!--    表示返回结果集
        id表示唯一表示
        type表示返回键结果集类型
        autoMapping表示自动映射，数据库有的字段与实体类会自动映射
-->
    <resultMap id="BaseAttrInfoMap" type="com.atguigu.gmall.model.product.BaseAttrInfo" autoMapping="true">
<!--
       id:表示主键
       property：表示实体类属性名称
       column:表示sql语句执行之后的列名
-->
        <id property="id" column="id"></id>
<!--

-->
<!--        表示普通字段   -->
<!--        <result property="" column="" ></result>-->
        <!--        mybatis如何配置1对多
         property：表示实体类属性名称
        ofType:表示多的那面返回的数据类型
-->
        <collection property="attrValueList" ofType="com.atguigu.gmall.model.product.BaseAttrValue" autoMapping="true">
<!--
            此时id发生了重名，起别名
-->
            <id property="id" column="attr_value_id"></id>
        </collection>

    </resultMap>
<!--    执行方法
        id表示方法名称
        resultMap返回结果集
-->
    <select id="selectBaseAttrInfoList" resultMap="BaseAttrInfoMap">
                    select
                            bai.attr_name,
                            bai.category_id,
                            bai.category_level,
                            bav.id attr_value_id,
                            bav.value_name,
                            bav.attr_id
                    from base_attr_info bai inner join base_attr_value bav
                    on bai.id = bav.attr_id
                   <where>
                       <if test="category1Id != null and category1Id != 0">
                       or (bai.category_id = #{category1Id} and bai.category_level = 1)

                   </if>
                       <if test="category2Id != null and category1Id != 0">
                           or (bai.category_id = #{category2Id} and bai.category_level = 2)

                       </if>
                       <if test="category3Id != null and category1Id != 0">
                           or (bai.category_id = #{category3Id} and bai.category_level = 3)

                       </if>
                   </where>
                    order by bai.category_level, bai.id

    </select>
</mapper>